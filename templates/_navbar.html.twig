<nav class="navbar navbar-expand-lg">
	<div class="container">
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			{% if is_granted("ROLE_USER") %}

				<ul class="navbar-nav me-auto">
					<li class="nav-item rounded bg-primary px-3 mx-2">
						<a class="nav-link active text-light" href="{{ path('app_tasks')}}">My Tasks</a>
					</li>
					<li class="nav-item rounded bg-primary px-3 mx-2">
						<a class="nav-link active text-light" href="{{ path('app_new_task')}}">New Task</a>
					</li>
				</ul>
				<ul class="navbar-nav">
					<li class="nav-item dropdown rounded bg-primary">
						<a class="nav-link dropdown-toggle text-light" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
							{{app.user.email}}
						</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('_logout_main')}}">Logout</a>
                        </li>
                </ul>

            {% else %}

                <ul class="navbar-nav">
					<li class="nav-item rounded bg-primary px-3 mx-2">
						<a class="nav-link active text-light" href="{{ path('app_login')}}">Login</a>
					</li>
					<li class="nav-item rounded bg-primary px-3 mx-2">
						<a class="nav-link active text-light" href="{{ path('app_registration')}}">Register</a>
					</li>
				</ul>

            {% endif %}
        </div>
    </div>
</nav>